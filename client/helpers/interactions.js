// Interactions = new Mongo.Collection('interactions');

// insert = function(message) {
//   Ineratctions.insert({message: message});
// };

// delete = function(interactionId){
//   // Interactions.delete()
// }

// Errors.insert({message: message});